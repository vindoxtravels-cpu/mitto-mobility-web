- Subtask definition and scope
  - List each subtask explicitly with a clear, user-facing title and one-sentence description of its purpose.
  - Specify the expected input and output for each subtask from a user perspective.
  - Define success criteria for each subtask (what the user sees when it’s done).

- User flow and interactions
  - Describe how a user discovers, starts, pauses, and completes each subtask.
  - Outline the sequence of steps for each subtask, including any confirmations or error messages users may encounter.
  - Define navigation between subtasks and how progress is shown.

- Progress tracking agnd status visibility
  - Provide a unified status for each subtask (e.g., Not Started, In Progress, Completed, Blocked).
  - Require a progress indicator per subtask where applicable (e.g., step count or percentage).
  - Specify how and where users can view overall progress across all subtasks.

- Notifications and messaging
  - Define when users should be notified about subtask events (start, completion, failure, required action).
  - Specify the channels and timing for notifications and the content displayed in-app.
  - Include user-facing messages for common outcomes and recoverable errors.

- Permissions and validation
  - Identify any prerequisites a user must meet before starting each subtask.
  - Specify validation rules users must satisfy to advance or complete each subtask.
  - Define how users are informed about missing requirements and what actions they can take to proceed.
